import {useEffect,useRef} from 'react';
export default function ParticleField(){const ref=useRef(null);useEffect(()=>{const c=ref.current,x=c.getContext('2d');let w,h,dpr,ps=[],mx=0,my=0;const resize=()=>{dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(dpr,0,0,dpr,0,0)};resize();ps=Array.from({length:Math.min(90,Math.floor(innerWidth/14))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25,r:Math.random()*1.5+.3}));const mm=e=>{mx=e.clientX;my=e.clientY};addEventListener('resize',resize);addEventListener('pointermove',mm,{passive:true});let id;const draw=()=>{x.clearRect(0,0,w,h);for(const p of ps){const dx=mx-p.x,dy=my-p.y,dist=Math.hypot(dx,dy);if(dist<150){p.vx+=dx/dist*.006;p.vy+=dy/dist*.006}p.x=(p.x+p.vx+w)%w;p.y=(p.y+p.vy+h)%h;x.beginPath();x.arc(p.x,p.y,p.r,0,Math.PI*2);x.fillStyle='#67e8f9aa';x.fill()}id=requestAnimationFrame(draw)};draw();return()=>{cancelAnimationFrame(id);removeEventListener('resize',resize);removeEventListener('pointermove',mm)}} ,[]);return <canvas ref={ref} className="fixed inset-0 z-0 opacity-60 pointer-events-none"/>}
